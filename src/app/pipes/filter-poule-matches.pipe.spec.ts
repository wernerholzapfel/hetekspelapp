import {FilterPouleMatchesPipe} from './filter-poule-matches.pipe';

describe('FilterPouleMatchesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPouleMatchesPipe();
    expect(pipe).toBeTruthy();
  });
});
