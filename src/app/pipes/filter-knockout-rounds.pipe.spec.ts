import { FilterKnockoutRoundsPipe } from './filter-knockout-rounds.pipe';

describe('FilterKnockoutRoundsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterKnockoutRoundsPipe();
    expect(pipe).toBeTruthy();
  });
});
